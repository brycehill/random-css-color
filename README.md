
### Dev
```sh
yarn && yarn dev
```

### Build
This will build a static site in `./build`
```sh
yarn build
```

### Deploy
Builds a new site and deploys it to now `css.bryce.fail`
```sh
yarn deploy
```
